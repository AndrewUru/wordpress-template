# Required: WordPress site URL (no trailing slash)
WP_URL=https://example.com

# Optional: WooCommerce REST API credentials
WC_CONSUMER_KEY=
WC_CONSUMER_SECRET=

# Optional: ISR revalidation interval in seconds
REVALIDATE_SECONDS=60

# Optional: Secret for /api/revalidate endpoint
WP_REVALIDATE_SECRET=

# Optional: Public site URL used in sitemap/metadata fallbacks
NEXT_PUBLIC_SITE_URL=http://localhost:3000
